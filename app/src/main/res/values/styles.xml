<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="HintTextError" parent="TextAppearance.Design.Error">
        <item name="android:fontFamily">@font/roboto_regular</item>
        <item name="colorAccent">@color/colorTextRed</item>
        <item name="colorControlNormal">@color/colorTextRed</item>
        <item name="colorControlActivated">@color/colorTextRed</item>
    </style>
    <style name="HintText" parent="TextAppearance.Design.Hint">
        <item name="android:fontFamily">@font/roboto_regular</item>
        <item name="colorAccent">@color/colorTextBlue</item>
        <item name="colorControlNormal">@color/colorTextBlue</item>
        <item name="colorControlActivated">@color/colorTextBlue</item>
    </style>
    <style name="AppThemebackground" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>
        <item name="colorAccent">@android:color/transparent</item>
        <item name="android:fontFamily">@font/roboto</item>
    </style>
    <style name="AppTheme.WhiteTextAppearance">
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
